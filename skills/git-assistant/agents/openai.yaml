interface:
  display_name: "Git Assistant"
  short_description: "Handle Git tasks with practical guidance"
  default_prompt: "Use $git-assistant for Git-related tasks; for commit suggestions, analyze staged or unstaged diffs; for commit execution, use only staged or latest relevant unstaged changes and ask explicit user confirmation before commit."
