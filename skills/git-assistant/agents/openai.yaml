interface:
  display_name: "Git Assistant"
  short_description: "Workspace choice and git closeout for all harnesses"
  default_prompt: "Use $git-assistant for Git tasks. Before implementation, read global then repo preferences for workspace choice. Closeout via closeout.md (commit, unify when worktree, push). Always confirm each git write phase."
