interface:
  display_name: "Project Context"
  short_description: "Reconcile docs/ with ## Docs; capture or refresh on ask."
  default_prompt: "Use $project-context to reconcile docs/ with AGENTS.md ## Docs, then capture or refresh docs/<topic>.md. Do not duplicate formatter/linter rules. Offer integrity-review when docs changed; ask before commit."
