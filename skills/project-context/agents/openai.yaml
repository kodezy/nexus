interface:
  display_name: "Project Context"
  short_description: "Grow flat docs/; update AGENTS.md ## Docs when asked."
  default_prompt: "Use $project-context to capture or refresh docs/<topic>.md. Add a ## Docs row for each new file. Do not duplicate formatter/linter rules. Ask before commit."
