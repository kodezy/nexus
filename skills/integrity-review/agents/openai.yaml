interface:
  display_name: "Integrity Review"
  short_description: "Review completed work, then confirm commit when clean."
  default_prompt: "Use $integrity-review to check the completed implementation, then offer commit confirmation when Clean or Corrected."
