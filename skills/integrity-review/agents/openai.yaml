interface:
  display_name: "Integrity Review"
  short_description: "Require evidence before done; verifier pass when available."
  default_prompt: "Use $integrity-review on the completed work. Run project checks, include a validation receipt, and use a separate verifier pass when the host supports it. Do not commit unless the user explicitly asks."
