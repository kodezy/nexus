interface:
  display_name: "Integrity Review"
  short_description: "Review completed work, then closeout when clean."
  default_prompt: "Use $integrity-review on the completed implementation, then hand off to git-assistant closeout when Clean or Corrected."
